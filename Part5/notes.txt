In order to install packages that are not available in node we can install
them with npm. 
We are going to install nodemon so we do not have to re run our server

Package.json file
If we will use any third party packages we should create package.json file 
run npm init 
Package-lock. json keeps track of different dependencie versions
